name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          # Laravel 12 corre el trait Dispatchable param-less (func_get_args): acá se
          # detecta el bug de args nombrados que un solo-13 no ve. Es la versión de rga.
          - laravel: '12.*'
            testbench: '10.*'
            php: '8.3'
          - laravel: '13.*'
            testbench: '11.*'
            php: '8.3'

    name: Laravel ${{ matrix.laravel }} (PHP ${{ matrix.php }})

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none

      - name: Instalar dependencias (fija la versión de Laravel/Testbench de la matriz)
        run: |
          composer require "laravel/framework:${{ matrix.laravel }}" "orchestra/testbench:${{ matrix.testbench }}" --no-interaction --no-update --dev
          composer update --prefer-dist --no-interaction --with-all-dependencies

      - name: Tests
        run: vendor/bin/phpunit --no-coverage
